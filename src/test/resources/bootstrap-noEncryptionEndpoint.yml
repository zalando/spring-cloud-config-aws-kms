aws:
  kms:
    useMock: false
    keyId: 'sample-key'
    region: eu-central-1  # ignored
    endpoint:
      service-endpoint: kms.us-east-1.amazonaws.com
      signing-region: us-east-2
spring:
  main:
    banner-mode: "off"
